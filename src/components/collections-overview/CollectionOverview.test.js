import Enzyme, { shallow } from "enzyme";
import Adapter from "@wojtekmaj/enzyme-adapter-react-17";

import CartItem from "./CartItem";

Enzyme.configure({ adapter: new Adapter() });

test("renders collection overview", () => {
  // TODO: add the test
});
